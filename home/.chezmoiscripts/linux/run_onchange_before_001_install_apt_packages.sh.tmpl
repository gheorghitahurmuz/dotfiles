#!/bin/bash

set -eufo pipefail

{{ $sudo := "sudo " -}}

{{ $packages := list
     "build-essential"
     "curl"
     "fuse" 
     "fzf"
     "git"
     "libfuse2"
     "zsh"
     "dirmngr"
     "gpg"
     "gawk"
-}}

{{ $sudo }}apt-get install -y {{ $packages | join " " }}