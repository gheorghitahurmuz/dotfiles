#!/bin/bash

set -eufo pipefail

{{ $sudo := "sudo " -}}

{{ $packages := list
     "build-essential"
     "curl"
     "fzf"
     "git"
     "zsh"
-}}

{{ $sudo }}apt-get install -y {{ $packages | join " " }}